'use strict'
/*

import {createContent, page} from './downloadingItems';

const search = document.querySelector('.search-block');

let searchByName = debounce((event) => {
    let valueSearched = event.target.value;
    let page = 0;
    createContent([], valueSearched , page , 20);
    console.log(valueSearched);
  }, 500);

search.addEventListener('input', searchByName);
*/